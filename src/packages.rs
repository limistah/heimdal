use anyhow::Result;
use crate::config::Profile;

/// Install packages for the given profile.
/// Full implementation in Phase 7 — stub for now.
pub fn install_for_profile(_profile: &Profile, _dry_run: bool) -> Result<()> {
    Ok(())
}
